class Block{
  
  float x,y,w,h;
  
  Block(float x,float y,float w,float h) {
    this.x = x;
    this.y = y;
    this.w = w;
    this.h = h;
  }
  
  void drawBlock() {
    //Picture thrown in later
  }
}
